// Physics
//event_inherited();

// Friction Test
//if (z == zFloor) {
	/* BUGGY WHEN ATTEMPTING TO PUSH DIAGONALLY
	if (xSpeed != 0) {
		xSpeed -= sign(xSpeed);
	}
	if (ySpeed != 0) {
		ySpeed -= sign(ySpeed);
	}
	*/
	//xSpeed = 0;
	//ySpeed = 0;
//}

// Hit Detection
default_hit();